syntax = "proto3";
package flustra.api.metrics;

message Metric {
  string name = 1;
  double value = 2;
  map<string, string> labels = 3;
}

message MetricsResponse {
  repeated Metric metrics = 1;
}

message Empty {}
